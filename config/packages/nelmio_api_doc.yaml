nelmio_api_doc:
    documentation:
        info:
            title: Bolton API
            description: Solution for the bolton backend challenge
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc\.?.*$) # Accepts routes under /api except /api/doc
